# Use the official Qdrant image
# Railway handles health checks via railway.toml
FROM qdrant/qdrant:v1.13.1

# No additional configuration needed
# The base image already includes everything required for Railway deployment
