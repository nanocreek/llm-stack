# Qdrant Vector Database Configuration
# Railway will auto-configure most networking and deployment settings

# API Key - Optional, for securing your vector database
# QDRANT_API_KEY=your_secure_api_key_here

# Storage Path - Where Qdrant stores vector data
# Typically handled by Railway volumes, but can be customized if needed
# QDRANT_STORAGE_PATH=/qdrant/storage

# Read-Only Mode - Set to true to run Qdrant in read-only mode
# QDRANT_READ_ONLY_MODE=false

# Snapshot Configuration
# Snapshots are automatic backups of your vector data
# QDRANT_SNAPSHOT_PATH=/qdrant/snapshots

# Performance Tuning - Memory limits (in MB)
# Max memory available to Qdrant
# QDRANT_MAX_MEMORY=1024

# Indexing and Search Configuration
# Number of threads for background operations
# QDRANT_INDEXING_THREADS=0

# Collection snapshots - Enable automatic snapshots
# QDRANT_ENABLE_COLLECTION_SNAPSHOTS=true

# Logging Level - trace, debug, info, warn, error
# QDRANT_LOG_LEVEL=info

# CORS Configuration - Enable cross-origin requests if needed
# QDRANT_CORS_ALLOWED_ORIGINS=*
