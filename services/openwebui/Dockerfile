# OpenWebUI service - using official Docker image
FROM ghcr.io/open-webui/open-webui:main

# Set environment variables for OpenWebUI
ENV PORT=8080

# Expose port 8080
EXPOSE 8080

# Use the base image's default entrypoint and CMD
# The official image is already properly configured
